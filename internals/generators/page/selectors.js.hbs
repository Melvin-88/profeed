/*
* {{ properCase name }} Selectors
*
* This contains all the text for the {{ properCase name }} component.
*/

import { createStructuredSelector } from 'reselect';
import { getFarmId } from 'helpers/selectorsHelper';

export const modelSelector = createStructuredSelector({
  permission : state => state.app.permissions.find((item) => item.name === ' && item.label === '),
  {{ lowerCase (removePage name) }}: state => state.{{ lowerCase (removePage name) }}Page.{{ lowerCase (removePage name) }},
  farmId : state => getFarmId(state)
});
