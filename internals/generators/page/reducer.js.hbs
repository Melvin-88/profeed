/*
* {{ properCase name }} Reducer
*
* This contains all the text for the {{ properCase name }} component.
*/
import * as actions from './actions.js';

const initialState = {
  {{ lowerCase (removePage name) }} : []
};

export default function (state = initialState, action = {}) {
  const { type, payload } = action;

  if (type === actions.{{ splitUpperByUnderscore name }}_SET) {
    return { ...state, {{ lowerCase (removePage name) }} : payload.{{ lowerCase (removePage name) }} };
  }

  return state;
}
