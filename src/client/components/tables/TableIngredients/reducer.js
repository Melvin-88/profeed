/*
* TableIngredients Reducer
*
* This contains all the text for the TableIngredients component.
*/
// import * as actions from './actions.js';

const initialState = {};

export default function (state = initialState) { // , action = {}
  // const { type, payload } = action;

  return state;
}
