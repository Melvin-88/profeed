/*
* TableTransfers Reducer
*
* This contains all the text for the TableTransfers component.
*/
// import * as actions from './actions.js';

const initialState = {};

export default function (state = initialState) { // , action = {}
  // const { type, payload } = action;

  return state;
}
